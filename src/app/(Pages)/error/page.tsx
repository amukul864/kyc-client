"use client";
import styles from "./page.module.css";

const ErrorPage = () => {
	return (
		<div className={`${styles.referenceNumber}`}>
			Invalid ShareID
		</div>
	);
};

export default ErrorPage;
